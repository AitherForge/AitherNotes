// Aither Backend configuration for Aither Notes.
// The shared backend is the only server-side bridge to Firebase and other Aither services.
window.AITHER_BACKEND_URL = window.AITHER_BACKEND_URL || 'https://aitherbackendnew.onrender.com';
